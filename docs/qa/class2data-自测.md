1. 课程计划维护列表要把课程计划的开始时间、结束时间展示出来，如果有的话  ✅
2. 课程计划维护列表上把孩子的头像展示出来 ✅
3. 在课程计划维护列表中添加了计划以后，切换到首页，计划没有立即更新到首页的日历课程计划中 ✅
4. 在课包中维护了课程包以后，切换回事首页，课程包信息没有立即更新到首页的计划列表中✅
5. 成长记录当没有记录的时候，应该展示“添加记录”，而不是展示添加课程
6. 首页的课程计划列表，现在只有开始时间，需要把开始时间和结束时间都展示在列表上✅
7. 首页日历默认改成月视图模式✅
8. 成长页面，如果上课记录、课时包等有照片时，不管是几张照片，照片的缩略图都显示成3张照片时的大小，不需要动态改变缩略图大大小✅
---
9. 首页点击录入成就，选择孩子时，孩子列表加上头像，如果只有一个孩子，就不需要进行选择，直接进入录入成就页面。在录入成就页面把，当前录入成就所属的孩子信息展示出来。同时为成就记录增加关联课程的选项，作为非必选项✅
10. 首页->录入课程,录入完成后，应该进入课程详情页。课程详情页应该有三个按钮，记录上课、录入课时包、添加上课计划，现在缺少了添加上课计划，这三个操作中，还是保持目前 记录上课时最主要的操作。录入课时包、添加上课计划也都应该像上课记录一样是浮层中操作。✅
11. 课程详情页中的统计数据未生效✅
12. 录入课程包时，也要可以上传照片，比如收据之类的✅
13. 成长页面，现在成长记录如果有附件，附件是一张照片、两张照片、三张照片等等时，照片的缩略图大小不一致，应该都是一样的。我的意思是，如果A记录只有一个照片，B记录有三个照片，那么A记录和B记录的照片的缩略图大小应该是一样的。就按现在3张照片时，每张照片的缩略图大小那样来设定。✅
14. 成长的统计页面，在页面顶上统计：总时长（所有课程的上课总时长，同样超过一定的600分钟以后，展示单位为小时）、总的花费，同时在每个课程中增加两个统计项：✅
- 时间占比：这门课的上课总时长/所有课程的上课总时长 * 100%，四舍五入保留两位小数即可。✅
- 花费占比：这门课的课时总花费/所有课程的课时花费 * 100%，四舍五入保留两位小数即可。✅
15. 课程计划维护列表要把课程计划的开始日期、结束日期展示出来，如果有的话，不展示开始时间和结束时间✅
16. 在课程详情->计划tab中，也需要把计划的开始日期、结束日期展示出来，如果有的话，不是展示开始时间和结束时间✅
---
17. 课程计划维护列表中，删除一个课程计划后，退出这个页面，再进入这个页面，刚才删除的计划还在，应该是缓存导致，因为我退出应用，再重新进入就没有计划了。✅
18. 成长页面，当没有任何记录时，点击添加记录，进入的是录入课程页面，应该是和首页->添加记录一样，弹出让用户选择为哪个课程添加记录的弹窗，如果还没有课程，那么才应该是进入到添加课程的页面。✅
19. 首页->录入课程，录入完成后，应该进入课程详情页面，不应该是回到首页。✅
20. 首页->录入课时包，弹窗中应该也要带上孩子信息（头像），区分是为哪个孩子的课程增加课时包✅
21. 录入课时包的详情页面，应该有孩子、课程的相关的信息，避免用户忘记当前是在为哪个孩子的哪个课录入课时包了✅
22. 删除孩子时，孩子的上课记录、成就附件也都一起删除掉，与孩子相关联的课程、与课程相关联的计划都一起删除掉✅
23. 在录入成就时，如果关联了某个课程，那么在成长页面展示这条成就记录的时候，这个记录类型，还是展示成就记录，而不是替换为关联的课程的名称✅
---
24. 成长记录的统计页面，再右下角增加一个浮层筛选按钮，点击之后，可以按照时间维度来进行过滤，对数据进行统计✅
25. 在首页日历中的课程计划列表中，把当前课程的课时包类型、所剩的所有课时数以标签形式展示出来✅
26. 在课程详情中，课包这个tab下有一个查看课时明细，这个功能是没必要的，因为记录中有课时的记录消耗， 所有这里可以去掉✅
27. 在课程详情中，课包这个tab下的课包列表的有一个作废的按钮，这个是做什么用的？作废了课包有什么影响？把作废改为删除，用户在点击删除时，这个课包没有关联任何课时，那么就直接删除，如果关联了课时，那么删除的时候，就提示用户，这个课包已经被使用，删除只是作废剩
余课时，历史消耗还是存在。✅
28. 首页日历中课程计划列表中的“待处理”改为“待记录”，待处理显得像是任务，我们这里是记录而不是任务。✅
29. 增加一个联系作者的功能，可以打赏可以联系我微信
30. 增加一个意见反馈功能
31. 把首页两个字改为上课✅
32. 在课程详情中的课包tab，课包列表上，如果是周期卡，不应该显示为0剩余，这个应该显示为未开始/进行中/已结束 吧，如果当前在有效期内就是有限，在开始有效期前，就是未开始，在结束有效期后，就是已结束。同时把生效开始时间、结束时间放展示状态后面✅
33. 上课记录的详情中，应该把消耗课时所属的课包也展示出来✅
34. 课程详情上，照片的展示一行就只展示三个，可以展示多行，不要都展示在一行上✅
35. 录入课程时，如果只有一个孩子，在孩子选项那里就默认选中这个孩子，多个孩子，就可以下拉选择✅



